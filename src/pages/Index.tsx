import { ATMSystem } from '@/components/ATMSystem';

const Index = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center p-4">
      <ATMSystem />
    </div>
  );
};

export default Index;
